<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>LitList</title>
</head>

<body>
    <p>story page testing</p>
    {% if lit %}
        <p>{{lit.id}}. {{lit.title}}</p>
        <p>
            author: {{lit.author}},
            rating: {{lit.rating}},
            commnet: {{lit.comment}},
            progress: {{lit.progress}},
            length: {{lit.length}},
            link: {{lit.link}}
        </p>
        <form action="/{{lit.id}}" method="post">
            <input type="hidden" name="_method" value="PUT" />
            <label for="title">Title:</label>
            <input type="text" name="title" />
            <button type="submit">Update Story</button>
        </form>
    {% endif %}
    <p><a href="/">Back</a></p>
</body>
